{% extends 'base.html.twig' %}

{% block title %}
    {{ parent() }} | todo:titre de la serie
{% endblock %}

{% block body %}
    <h3> titre de la serie </h3>

    <section class="series-list">
        {% for serie in series %}
            <article>
                <a href="{{ path('series_details',{'id' : serie.id()}) }}">
                    <img src="{{ asset('img/posters/series/'~serie.poster) }}" alt="{{ serie.name }}">
                </a>

            </article>
        {% endfor %}
    </section>


{% endblock %}

